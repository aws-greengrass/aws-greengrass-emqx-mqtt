--- emqx
+++ emqx
@@ -775,6 +775,6 @@
 
     ## Merge hocon generated *.args into the vm.args
     TTMP_ARG_FILE="$CONFIGS_DIR/vm.args.tmp"
-    cp "$EMQX_ETC_DIR/vm.args" "$TMP_ARG_FILE"
+    cat "$EMQX_ETC_DIR/vm.args" > "$TMP_ARG_FILE"
     echo "" >> "$TMP_ARG_FILE"
     echo "-pa \"${REL_DIR}/consolidated\"" >> "$TMP_ARG_FILE"
